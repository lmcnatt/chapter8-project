# Database (RDS MySQL) — copy to .env and set values. Do not commit .env.
# For local MySQL use localhost; for RDS use terraform output rds_endpoint.
DB_HOST=localhost
DB_PORT=3306
DB_NAME=icecream
DB_USER=admin
DB_PASSWORD=
DB_SSL=false

# Server
PORT=3000
